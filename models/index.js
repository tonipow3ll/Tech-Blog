const User = require('./user');
// const Post = require('./post');

// User.hasMany(Post, {
//     foreignKey: 'postId',
// })

// Post.belongsTo(User, {
//     foreignKey: 'postId',
// })

module.exports = { User }